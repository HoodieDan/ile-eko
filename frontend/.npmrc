# React Native + Metro need a flat-ish node_modules layout. Hoisting native
# dependencies avoids Metro resolution issues from deeply-nested copies.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
