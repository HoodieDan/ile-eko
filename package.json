{
  "name": "ile-eko",
  "version": "0.1.0",
  "private": true,
  "description": "Ilé Èkó — Lagos rental management. Expo frontend monorepo (frontend/) + reserved backend (backend/).",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  },
  "packageManager": "pnpm@10.18.3",
  "engines": {
    "node": ">=20"
  }
}
