# Ilé Èkó — Backend (reserved)

This directory is reserved for the Node.js + Express + MongoDB Atlas + OpenAI backend that powers both mobile apps. It is intentionally empty until backend work begins.

Shared TypeScript types live in `frontend/packages/core/src/types` and are designed to be imported here once this project exists.
